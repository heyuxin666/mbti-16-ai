[build]
  publish = "."

[functions]
  directory = "netlify/functions"

# Proxy /api/* to Netlify Functions, keep URL unchanged in browser
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
